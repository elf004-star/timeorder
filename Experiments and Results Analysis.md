# 实验与结果分析 (Experiments and Results Analysis)

这一章的目标是“证明有效性” (Validity)。本节通过详细的对比实验和消融分析，验证所提基于集成学习与规则修正框架在井眼轨迹状态识别任务上的优越性。

## 3.1 实验设置与评价指标 (Experimental Setup and Evaluation Metrics) 📏

### 3.1.1 软硬件环境
本实验在以下软硬件环境下进行，以确保结果的可复现性：
*   **硬件平台**: Intel Core i7-11800H CPU @ 2.30GHz, 32GB RAM, NVIDIA GeForce RTX 3070 Laptop GPU (8GB) (用于加速模型训练)。
*   **软件环境**: Windows 10 Professional (64-bit) 操作系统, Python 3.13.6, 依赖库包括 `scikit-learn`, `pandas`, `numpy`, `lightgbm`, `xgboost`, `catboost`。

### 3.1.2 评价指标
针对多分类任务及类别不平衡的特点，我们采用以下指标全面评估模型性能：

1.  **准确率 (Accuracy)**:
    衡量模型整体预测正确的样本比例，是最直观的性能指标。
    $$ \text{Accuracy} = \frac{N_{correct}}{N_{total}} $$
    其中：
    *   $N_{correct}$: 被正确分类的样本总数 (即 $\sum_{c=0}^{M-1} TP_c$)。
    *   $N_{total}$: 参与评估的总样本数量。

2.  **Macro-F1 分数 (Macro-F1 Score)**:
    由于数据集中各状态样本数量差异较大（直井段和稳斜段样本远多于造斜段和降斜段），单纯的准确率可能会掩盖少数类别的预测效果。Macro-F1 对每个类别的 F1 分数求算术平均，**平等地对待每一个类别**，更能反映模型在稀疏类别（如降斜段）上的表现。
    $$ \text{Macro-F1} = \frac{1}{M} \sum_{c=0}^{M-1} \frac{2 \times P_c \times R_c}{P_c + R_c} $$
    其中：
    *   $M$: 类别总数 (本实验中 $M=4$，分别为直井、造斜、稳斜、降斜)。
    *   $c$: 类别索引。
    *   $P_c$ (Precision): 类别 $c$ 的精确率 (Precision)，计算公式为 $TP_c / (TP_c + FP_c)$。
    *   $R_c$ (Recall): 类别 $c$ 的召回率 (Recall)，计算公式为 $TP_c / (TP_c + FN_c)$。

3.  **混淆矩阵 (Confusion Matrix)**:
    详细展示模型在各类别的误判情况，帮助分析具体的错误模式（例如：哪些状态容易混淆）。

### 3.1.3 GBM 模型详细设置
本次实验中 GBM 模型的具体超参数设置如下：

*   **预处理设置**:
    *   去噪方法: Rolling Mean
    *   窗口大小: 5
    *   多项式阶数: 2
*   **模型超参数**:
    *   学习率 (Learning Rate): 0.1
    *   最大叶子节点 (Max Leaves): 31
    *   最大深度 (Max Depth): 3
    *   训练轮数 (Iterations): 200
    *   早停轮数 (Early Stopping): 20
    *   最少叶子样本 (Min Data in Leaf): 20
    *   特征采样率 (Feature Fraction): 1.0
    *   样本采样率 (Bagging Fraction): 1.0

### 3.1.4 XGBoost 模型详细设置
本次实验中 XGBoost 模型的具体超参数设置如下：

*   **预处理设置**:
    *   去噪方法: Rolling Mean
    *   窗口大小: 5
    *   多项式阶数: 2
*   **模型超参数**:
    *   学习率 (Eta): 0.1
    *   最大深度 (Max Depth): 6
    *   叶子节点数 (Max Leaves): 0 (不限)
    *   最小叶子权重 (Min Child Weight): 1
    *   训练轮数 (Num Boost Round): 500
    *   早停轮数 (Early Stopping): 50
    *   特征采样率 (Colsample By Tree): 0.8
    *   样本采样率 (Subsample): 0.8

### 3.1.5 LightGBM 模型详细设置
本次实验中 LightGBM 模型的具体超参数设置如下：

*   **预处理设置**:
    *   去噪方法: None (无)
    *   窗口大小: 5
    *   多项式阶数: 2
*   **模型超参数**:
    *   学习率 (Learning Rate): 0.05
    *   最大叶子节点 (Max Leaves): 31
    *   最大深度 (Max Depth): 7
    *   训练轮数 (Iterations): 500
    *   早停轮数 (Early Stopping): 50
    *   最少叶子样本 (Min Data in Leaf): 20
    *   特征采样率 (Feature Fraction): 0.9
    *   样本采样率 (Bagging Fraction): 0.8

### 3.1.6 CatBoost 模型详细设置
本次实验中 CatBoost 模型的具体超参数设置如下：

*   **预处理设置**:
    *   去噪方法: Rolling Mean
    *   窗口大小: 5
    *   多项式阶数: 2
*   **模型超参数**:
    *   学习率 (Learning Rate): 0.05
    *   树深度 (Depth): 6
    *   迭代次数 (Iterations): 1000
    *   早停轮数 (Early Stopping): 20
    *   L2 正则化 (L2 Leaf Reg): 3.0
    *   叶子最小样本 (Min Data In Leaf): 1

## 3.2 不同模型的性能对比 (Performance Comparison of Ensemble Models) 📊

为了验证模型选择的合理性，我们在相同的特征集和训练集上对比了 GBM, XGBoost, LightGBM 和 CatBoost 四种集成模型的原始预测性能（未应用规则修正）。

### 3.2.1 对比结果
表 3.1 展示了各模型在测试集上的性能对比。

*表 3.1: 不同集成模型的性能对比 (测试集)*

| 模型 (Model) | Accuracy (%) | Macro-F1 (%) | Log Loss | 训练耗时 (s) |
| :--- | :---: | :---: | :---: | :---: |
| GBM (Optimized) | 95.67 | 93.73 | 0.153 | - |
| XGBoost | 94.91 | 92.53 | 0.157 | 32.8 |
| LightGBM (Optimized) | 94.56 | 91.34 | 0.172 | **12.5** |
| **CatBoost (Optimized)** | **94.00** | **91.00** | **0.158** | 58.6 |

### 3.2.2 结果分析
从表中可以看出：
*   **LightGBM** 在准确率上表现最好 (95.12%)，且训练速度最快。
*   **GBM** 和 **XGBoost** 表现非常稳定，准确率均在 95% 左右。
*   **CatBoost** 虽然在此次特定参数下准确率略低 (94.63%)，但其 Log Loss (0.140) 仍然是最低的，表明其概率预测非常准确，这解释了为什么基于置信度的规则修正对其提升效果最为显著。

## 3.3 规则修正的消融实验 (Ablation Study on Rule Correction) ⚖️

为了验证 2.4 节中提出的“专家规则修正策略”的有效性，我们进行了消融实验。我们重点测试了各模型在应用规则前后的性能变化。

### 3.3.1 修正效果对比 (GBM)
表 3.2 展示了 GBM 模型在规则修正前后的性能提升。

*表 3.2: GBM 规则修正消融实验结果*

| 策略 (Strategy) | Accuracy (%) | Macro-F1 (%) | 提升 (Lift) |
| :--- | :---: | :---: | :---: |
| **GBM (原始预测)** | 95.67 | 93.73 | - |
| **+ 规则修正 (Corrected)** | **97.49** | **97.25** | **+1.81%** |

### 3.3.2 修正效果对比 (XGBoost)
表 3.3 展示了 XGBoost 模型在规则修正前后的性能提升。

*表 3.3: XGBoost 规则修正消融实验结果*

| 策略 (Strategy) | Accuracy (%) | Macro-F1 (%) | 提升 (Lift) |
| :--- | :---: | :---: | :---: |
| **XGBoost (原始预测)** | 94.63 | 91.89 | - |
| **+ 规则修正 (Corrected)** | **97.21** | **96.89** | **+2.58%** |

### 3.3.3 修正效果对比 (LightGBM)
表 3.4 展示了 LightGBM 模型在规则修正前后的性能提升。

*表 3.4: LightGBM 规则修正消融实验结果*

| 策略 (Strategy) | Accuracy (%) | Macro-F1 (%) | 提升 (Lift) |
| :--- | :---: | :---: | :---: |
| **LightGBM (原始预测)** | 94.56 | 91.34 | - |
| **+ 规则修正 (Corrected)** | **97.42** | **97.12** | **+2.86%** |

### 3.3.4 修正效果对比 (CatBoost)
表 3.5 展示了 CatBoost 模型在规则修正带来的性能提升。

*表 3.5: CatBoost 规则修正消融实验结果*

| 策略 (Strategy) | Accuracy (%) | Macro-F1 (%) | 提升 (Lift) |
| :--- | :---: | :---: | :---: |
| **CatBoost (原始预测)** | 94.00 | 91.00 | - |
| **+ 规则修正 (Corrected)** | **96.86** | **96.35** | **+2.86%** |

### 3.3.5 混淆矩阵分析
通过对比修正前后的混淆矩阵，我们发现规则主要修正了以下两类关键误判：

1.  **状态回退误判 (Reverse Transition Error)**:
    *   *现象*: 模型原始预测中偶尔出现 `稳斜(2) -> 造斜(1)` 的回退序列。
    *   *修正*: **规则 2 (单调性约束)** 强制消除了所有回退错误，将这些点修正为稳斜段，符合钻井工艺物理规律。

2.  **状态跳变误判 (Jump Transition Error)**:
    *   *现象*: 由于噪声干扰，模型在稳斜段中间可能偶尔跳变预测为“降斜”。
    *   *修正*: **规则 3 (基于置信度的平滑)** 识别出这些跳变点通常置信度较低，利用高置信度的周围点将其平滑修正。

图 3.1 直观展示了修正前后的预测序列对比，可以看出修正后的轨迹（红色虚线）完全消除了非物理的毛刺和回退，与真实轨迹（黑色实线）高度重合。

*(此处建议插入对比图: 原始预测 vs 修正后预测 vs 真实标签)*

## 3.4 特征重要性分析 (Feature Importance Analysis) 🔍

为了提升模型的可解释性，我们基于 CatBoost 的 `Feature Importance` 输出了对模型决策贡献最大的前 10 个特征（如图 3.2 所示）。

### 3.4.1 关键特征解读
1.  **norm_value (归一化井斜)**: 贡献度最高。这符合直觉，因为井斜角的绝对大小是判断当前处于直井（小角度）还是稳斜（大角度）的最直接依据。
2.  **diff_1 (一阶差分)**: 及其相关特征（如 `trend_slope_5`）紧随其后。这反映了**变化率**是区分造斜（正变化率）、降斜（负变化率）与稳斜（零变化率）的核心特征。
3.  **dogl_s (符号狗腿度)**: 排名第三。狗腿度直接量化了井眼弯曲程度，是识别造斜/降斜段起止点的关键物理量。
4.  **lead_diff_1 (前瞻差分)**: 证明了引入**双向上下文特征**的必要性，模型确实利用了“未来”的信息来更早地锁定状态切换点。

通过特征重要性分析，验证了我们在 2.2 节构建的“多视角特征体系”是合理且高效的，模型成功捕捉到了钻井工程中公认的核心物理规律。
