# 实验与结果分析 (Experiments and Results Analysis)

这一章的目标是“证明有效性” (Validity)。本节通过详细的对比实验和消融分析，验证所提基于集成学习与规则修正框架在井眼轨迹状态识别任务上的优越性。

## 3.1 实验设置与评价指标 (Experimental Setup and Evaluation Metrics) 📏

### 3.1.1 软硬件环境
本实验在以下软硬件环境下进行，以确保结果的可复现性：
*   **硬件平台**: Intel Core i7-11800H CPU @ 2.30GHz, 32GB RAM, NVIDIA GeForce RTX 3070 Laptop GPU (8GB) (用于加速模型训练)。
*   **软件环境**: Windows 10 Professional (64-bit) 操作系统, Python 3.13.6, 依赖库包括 `scikit-learn`, `pandas`, `numpy`, `lightgbm`, `xgboost`, `catboost`。

### 3.1.2 评价指标
针对多分类任务及类别不平衡的特点，我们采用以下指标全面评估模型性能：

1.  **准确率 (Accuracy)**:
    衡量模型整体预测正确的样本比例，是最直观的性能指标。
    $$ \text{Accuracy} = \frac{N_{correct}}{N_{total}} $$
    其中：
    *   $N_{correct}$: 被正确分类的样本总数 (即 $\sum_{c=0}^{M-1} TP_c$)。
    *   $N_{total}$: 参与评估的总样本数量。

2.  **Macro-F1 分数 (Macro-F1 Score)**:
    由于数据集中各状态样本数量差异较大（直井段和稳斜段样本远多于造斜段和降斜段），单纯的准确率可能会掩盖少数类别的预测效果。Macro-F1 对每个类别的 F1 分数求算术平均，**平等地对待每一个类别**，更能反映模型在稀疏类别（如降斜段）上的表现。
    $$ \text{Macro-F1} = \frac{1}{M} \sum_{c=0}^{M-1} \frac{2 \times P_c \times R_c}{P_c + R_c} $$
    其中：
    *   $M$: 类别总数 (本实验中 $M=4$，分别为直井、造斜、稳斜、降斜)。
    *   $c$: 类别索引。
    *   $P_c$ (Precision): 类别 $c$ 的精确率 (Precision)，计算公式为 $TP_c / (TP_c + FP_c)$。
    *   $R_c$ (Recall): 类别 $c$ 的召回率 (Recall)，计算公式为 $TP_c / (TP_c + FN_c)$。

3.  **混淆矩阵 (Confusion Matrix)**:
    详细展示模型在各类别的误判情况，帮助分析具体的错误模式（例如：哪些状态容易混淆）。

### 3.1.3 各模型超参数详细设置 (Detailed Hyperparameters)
本次实验中各集成模型的具体超参数设置汇总如表 3.1 所示。

*表 3.1: 各集成模型超参数设置汇总*

| 参数设置 (Parameter Settings) | GBM | XGBoost | LightGBM | CatBoost |
| :--- | :---: | :---: | :---: | :---: |
| **预处理: 去噪方法** | Rolling Mean | Rolling Mean | None (无) | Rolling Mean |
| **预处理: 窗口大小** | 5 | 5 | 5 | 5 |
| **预处理: 多项式阶数** | 2 | 2 | 2 | 2 |
| **模型: 学习率 (Learning Rate)** | 0.1 | 0.1 | 0.05 | 0.05 |
| **模型: 最大叶子节点 (Max Leaves)** | 31 | 0 (不限) | 31 | / |
| **模型: 最大深度 (Max Depth)** | 3 | 6 | 7 | 6 |
| **模型: 训练轮数 (Iterations)** | 200 | 500 | 500 | 1000 |
| **模型: 早停轮数 (Early Stopping)** | 20 | 50 | 50 | 20 |
| **模型: 最少叶子样本/权重** | 20 | 1 | 20 | 1 |
| **模型: 特征采样率** | 1.0 | 0.8 | 0.9 | / |
| **模型: 样本采样率** | 1.0 | 0.8 | 0.8 | / |
| **模型: L2 正则化** | / | / | / | 3.0 |

## 3.2 不同模型的性能对比 (Performance Comparison of Ensemble Models) 📊

为了验证模型选择的合理性，我们在相同的特征集和训练集上对比了 GBM, XGBoost, LightGBM 和 CatBoost 四种集成模型的原始预测性能（未应用规则修正）。

### 3.2.1 对比结果
表 3.2 展示了各模型在测试集上的性能对比。

*表 3.2: 不同集成模型的性能对比 (测试集)*

| 模型 (Model) | Accuracy (%) | Macro-F1 (%) | Log Loss | 训练耗时 (s) |
| :--- | :---: | :---: | :---: | :---: |
| GBM (Optimized) | 95.67 | 93.73 | 0.153 | 26 |
| XGBoost | 94.91 | 92.53 | 0.157 | 9 |
| LightGBM (Optimized) | 94.56 | 91.34 | 0.172 | 10 |
| CatBoost (Optimized) | 94.00 | 91.00 | 0.158 | 9 |

### 3.2.2 结果分析
从表中可以看出：
*   **GBM** 在各方面指标上均表现最佳，准确率 (95.67%) 和 Macro-F1 (93.73%) 最高，且 Log Loss (0.153) 最低，表明其预测性能最强且概率估计最准确。虽然其训练耗时 (26s) 相对较长，但仍在可接受范围内。
*   **XGBoost** 表现出极佳的平衡性，准确率 (94.91%) 紧随 GBM 之后，且训练速度最快 (9s)，适合对实时性要求较高的场景。
*   **LightGBM** 和 **CatBoost** 的训练速度也非常快 (分别为 10s 和 9s)，虽然准确率略低于 GBM 和 XGBoost，但整体性能依然保持在高水平，尤其是 CatBoost 在保持极快训练速度的同时，Log Loss (0.158) 也控制得较好。

## 3.3 规则修正的消融实验 (Ablation Study on Rule Correction) ⚖️

为了验证 2.4 节中提出的“专家规则修正策略”的有效性，我们对比了各模型在应用规则修正前后的性能提升。表 3.3 汇总了所有模型的修正效果。

*表 3.3: 规则修正前后的性能提升对比*

| 策略 (Strategy) | Accuracy (%) | 提升 (Lift) | Macro-F1 (%) | 提升 (Lift) |
| :--- | :---: | :---: | :---: | :---: |
| **GBM + Rule Correction** | 97.49 | +1.82% | 97.25 | +3.52% |
| **XGBoost + Rule Correction** | 97.21 | +2.30% | 96.89 | +4.36% |
| **LightGBM + Rule Correction** | 97.42 | +2.86% | 97.12 | +5.78% |
| **CatBoost + Rule Correction** | 96.86 | +2.86% | 96.35 | +5.35% |

### 3.3.1 结果分析
从表 3.3 可以看出，引入专家规则修正后，所有模型的性能均有显著提升。其中，**LightGBM** 和 **CatBoost** 的准确率提升幅度最大（均为 +2.86%），且 Macro-F1 的提升更为明显（超过 5%），说明规则修正有效地改善了模型在少样本类别上的预测效果。**GBM** 在修正后准确率达到最高的 97.49%，证明了“集成学习 + 规则修正”框架的优越性。

### 3.3.5 混淆矩阵分析
通过对比修正前后的混淆矩阵，我们发现规则主要修正了以下两类关键误判：

1.  **状态回退误判 (Reverse Transition Error)**:
    *   *现象*: 模型原始预测中偶尔出现 `稳斜(2) -> 造斜(1)` 的回退序列。
    *   *修正*: **规则 2 (单调性约束)** 强制消除了所有回退错误，将这些点修正为稳斜段，符合钻井工艺物理规律。

2.  **状态跳变误判 (Jump Transition Error)**:
    *   *现象*: 由于噪声干扰，模型在稳斜段中间可能偶尔跳变预测为“降斜”。
    *   *修正*: **规则 3 (基于置信度的平滑)** 识别出这些跳变点通常置信度较低，利用高置信度的周围点将其平滑修正。

图 3.1 直观展示了修正前后的预测序列对比，可以看出修正后的轨迹（红色虚线）完全消除了非物理的毛刺和回退，与真实轨迹（黑色实线）高度重合。

*(此处建议插入对比图: 原始预测 vs 修正后预测 vs 真实标签)*

## 3.4 特征重要性分析 (Feature Importance Analysis) 🔍

为了提升模型的可解释性，我们提取了 GBM、XGBoost、LightGBM 和 CatBoost 四个模型中决策贡献最大的前 10 个特征（如图 3.2 所示，a 为 GBM，b 为 XGBoost，c 为 LightGBM，d 为 CatBoost）。不同模型对特征的偏好略有不同，但核心特征表现出高度的一致性。

### 3.4.1 关键特征解读
1.  **dogl_s_cumsum (累计符号狗腿度)**: 在 GBM (a) 和 LightGBM (c) 中位列第一，在其他模型中也极具影响力。该特征通过积分狗腿度，有效捕捉了轨迹的**累积方向变化**，是判断当前处于“增斜过程积累”还是“稳斜保持”状态的最强信号。
2.  **dist_to_max (距离最大井斜差值)**: 在多个模型中稳居前三。作为一个全局位置特征，它衡量当前井斜与最大井斜的差距，帮助模型快速定位是否接近或处于稳斜段（通常对应最大井斜区域）。
3.  **accum_ratio (累计井斜占比)**: 在 CatBoost (d) 中贡献最高。它反映了当前累积的井斜幅度占全井的比例，是一个强有力的**进程指示器**，辅助模型判断钻进阶段。
4.  **dogl_s_mean_5 (符号狗腿度均值)** 与 **center_diff (中心差分)**: 这一点在 XGBoost (b) 中尤为突出（`center_diff` 排名第一）。这些特征直接量化了局部的**变化率与弯曲度**，是精准识别造斜/降斜段起止点（状态切换边界）的关键物理依据。

通过特征重要性分析，验证了我们在 2.2 节构建的“多视角特征体系”是合理且高效的，模型不仅捕捉到了局部的物理变化（如狗腿度），更学会了利用全局上下文（如累计占比、最大值距离）来进行综合决策。
