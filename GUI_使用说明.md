# 井段状态预测模型训练系统 - GUI版本使用说明

## 功能特性

本GUI训练系统提供了以下功能：

1. **可视化界面操作** - 直观的图形界面，无需命令行操作
2. **灵活的数据文件选择** - 支持选择任意CSV文件进行训练
3. **数据集划分配置** - 可自定义训练集、验证集和测试集的比例
4. **超参数调整** - 实时调整模型的各项超参数
5. **实时训练监控** - 动态显示训练和验证损失曲线
6. **详细日志记录** - 完整记录训练过程的每一步

## 运行要求

确保已安装以下Python包：
```bash
pip install pandas numpy scikit-learn lightgbm matplotlib seaborn
```

## 启动程序

运行GUI版本的训练程序：
```bash
python train_model_gui.py
```

## 界面说明

### 左侧控制面板

#### 1. 数据文件选择
- **数据文件**: 默认为 `data001.csv`
- 点击"浏览..."按钮可以选择其他CSV文件

#### 2. 数据集划分
- **测试集比例**: 调整测试集占总数据的比例（范围：0.1-0.4，默认：0.2）
- **验证集比例**: 调整验证集占总数据的比例（范围：0.05-0.3，默认：0.1）

> **注意**: 程序会自动按井为单位进行数据划分，确保同一口井的数据不会被分散到不同的数据集中。

#### 3. 模型超参数

可调整的超参数包括：

| 参数名称 | 说明 | 默认值 | 建议范围 |
|---------|------|--------|---------|
| 学习率 | 控制模型学习速度 | 0.05 | 0.01-0.1 |
| 叶子节点数 | 决策树的复杂度 | 31 | 15-63 |
| 最大深度 | 树的最大深度 | 7 | 3-12 |
| 训练轮数 | 最大迭代次数 | 500 | 100-1000 |
| 早停轮数 | 多少轮不改善则停止 | 50 | 20-100 |
| 最小样本数 | 叶子节点最小样本数 | 20 | 10-50 |
| 特征采样率 | 每次迭代使用的特征比例 | 0.9 | 0.5-1.0 |
| 样本采样率 | 每次迭代使用的样本比例 | 0.8 | 0.5-1.0 |

### 右侧显示面板

#### 1. 训练监控标签页
- 实时显示训练损失和验证损失曲线
- 动态更新，可以观察模型收敛过程
- 蓝色曲线：训练集损失
- 红色曲线：验证集损失

#### 2. 训练日志标签页
- 显示详细的训练日志信息
- 包括数据加载、特征工程、训练进度等信息

#### 3. 训练结果标签页
- 显示最终的测试集评估结果
- 包括准确率、F1分数等指标
- 展示规则修正前后的性能对比

## 使用流程

### 基本使用步骤

1. **启动程序**
   ```bash
   python train_model_gui.py
   ```

2. **选择数据文件**（可选）
   - 如果使用默认的 `data001.csv`，可跳过此步
   - 否则点击"浏览..."按钮选择CSV文件

3. **配置数据集划分**（可选）
   - 根据需要调整测试集和验证集的比例
   - 建议保持默认值

4. **调整超参数**（可选）
   - 如果是首次训练，建议使用默认参数
   - 有经验的用户可以根据需要调整

5. **开始训练**
   - 点击"开始训练"按钮
   - 观察训练监控页面的损失曲线
   - 等待训练完成

6. **查看结果**
   - 训练完成后会弹出提示框
   - 切换到"训练结果"标签页查看详细结果
   - 模型会自动保存为 `well_status_model.txt`

### 超参数调优建议

#### 如果模型欠拟合（训练和验证损失都很高）：
- 增加 `叶子节点数`
- 增加 `最大深度`
- 减小 `最小样本数`
- 增加 `训练轮数`

#### 如果模型过拟合（训练损失低但验证损失高）：
- 减小 `叶子节点数`
- 减小 `最大深度`
- 增加 `最小样本数`
- 减小 `特征采样率` 和 `样本采样率`
- 增加正则化

#### 如果训练速度太慢：
- 增加 `学习率`
- 减小 `训练轮数`
- 减小 `叶子节点数`

#### 如果训练速度太快导致不稳定：
- 减小 `学习率`
- 增加 `训练轮数`

## 输出文件

训练完成后会生成以下文件：

1. **well_status_model.txt** - 训练好的LightGBM模型
2. **feature_importance.png** - 特征重要性图表（如果实现了）
3. **Confusion_Matrix_Raw.png** - 原始预测的混淆矩阵（如果实现了）
4. **Confusion_Matrix_Corrected.png** - 规则修正后的混淆矩阵（如果实现了）

## 注意事项

1. **数据格式要求**：
   - CSV文件必须包含以下列：`序号`, `转换后JH`, `JX`, `dy➗dx`, `status`
   - `status` 列的值应为 0, 1, 2, 3（分别代表直井段、造斜段、稳斜段、降斜段）

2. **训练时间**：
   - 训练时间取决于数据量和超参数设置
   - 大约需要几分钟到十几分钟不等
   - 训练过程中不要关闭窗口

3. **内存使用**：
   - 确保有足够的内存用于加载数据和训练模型
   - 如果数据量很大，建议关闭其他占用内存的程序

4. **线程安全**：
   - 训练在独立线程中运行，不会阻塞UI
   - 训练过程中可以查看日志和监控曲线
   - 但请勿在训练过程中修改参数或启动新的训练

## 常见问题

### Q: 程序提示"文件不存在"
A: 检查文件路径是否正确，确保CSV文件存在于指定位置

### Q: 训练曲线不更新
A: 这可能是由于线程同步问题，通常会在下一次迭代时更新

### Q: 如何使用训练好的模型进行预测？
A: 可以参考原始的 `train_model.py` 中的 `predict_well()` 方法，加载保存的模型文件进行预测

### Q: 可以中途停止训练吗？
A: 当前版本不支持中途停止，建议设置较小的训练轮数或早停轮数

## 技术支持

如有问题或建议，请联系开发团队。

---

**版本**: 1.0  
**最后更新**: 2025年10月

